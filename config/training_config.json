{
  "learning_type": "supervised",
  "config": {
    "batch_size": 8,
    "learning_rate": 2e-5,
    "num_train_epochs": 3,
    "weight_decay": 0.01,
    "warmup_steps": 500,
    "max_seq_length": 128,
    "save_steps": 10000,
    "evaluation_strategy": "epoch"
  }
}